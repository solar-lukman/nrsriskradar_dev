# E2E coverage report

Generated by `node scripts/e2e-coverage.mjs` — do not edit by hand.
Source of truth: `docs/uat-test-plan.md` and the Playwright specs in `e2e/tests/`.
See `docs/uat-traceability.md` for the one-line matrix view.

## Summary

| Metric | Value |
| --- | --- |
| Plan cases | 73 |
| Automated | 73 (100%) |
| Manual only | 0 |
| Spec files | 25 |
| Annotated tests | 96 |
| Extra automated checks (no plan case) | 20 |
| Orphan annotations | 0 |

## Baseline seed data (plan Section 8)

| Area | Required test data |
| --- | --- |
| Accounts | One account per role (RC, RR, RO, RMD, CRO, ERMSC, EC, RCB, SUPERVISOR, ADMIN, USER), each in a named department |
| Lookups | ≥ 5 risk categories, ≥ 3 departments, ≥ 3 strategic objectives, 5×5 scoring matrix |
| Risks | ≥ 10 risks spanning Draft, Submitted, Under Review, Approved, Escalated, Mitigated; at least 2 with residual score ≥ 15 |
| Treatment | ≥ 3 mitigation tasks with NGN budgets, one > 90% utilised, one overdue |
| Appetite | Two appetite rows: tolerant category and low-tolerance category (threshold 12) |
| KRIs | Two indicators (one "higher is worse", one "lower is worse"), one with a past next-due date, one linked to a risk |
| Governance | One Approved and one Escalated risk available for committee decisions; minute reference format `ERMSC/UAT/001` |
| BCP | ≥ 3 plans covering Ready / Needs Review / Outdated, one with a scheduled future test |
| Incidents | ≥ 2 crystallised risk events with owners assigned |
| Whistleblowing | Two anonymous cases: one fresh, one > 14 days unassigned for the SLA flag |

Every case ID is mapped to a data factory in `e2e/fixtures/caseData.ts`; run
`node scripts/seed-e2e-data.mjs --case <ID>` (or `npm run test:e2e:seed`) to
provision it. The full matrix is in `docs/e2e-test-data.md`.

Tests guard themselves with `test.skip(...)` when a fixture is absent, so a thin
dataset silently reduces effective coverage — the "Required test data" line under
each case lists exactly what must exist for that test to execute rather than skip.

## Case-by-case coverage

## 5.1 Authentication & Access Control

### UAT-AUTH-01 — P1 — Any

- **Plan step:** Log in with valid credentials
- **Expected:** Lands on role-appropriate dashboard within 5s
- **Spec:** `e2e/tests/auth.spec.ts` → `UAT-AUTH-01 [${user.role}] logs in with valid credentials`
  - Assertions:
    - `expect(await isOnLogin(page)).toBe(false)`
  - Required test data / skip guards: none beyond the seeded baseline
- **Spec:** `e2e/tests/role-landing.spec.ts` → `[${user.role}] lands on ${user.landing} via /`
  - Assertions:
    - `expect.soft(url.pathname, `Expected ${user.role} to land on ${user.landing}`).toBe(user.landing)`
  - Required test data / skip guards: none beyond the seeded baseline

### UAT-AUTH-02 — P1 — Any

- **Plan step:** Log in with wrong password 5×
- **Expected:** Account is locked; `admin-invite`/reset flow required
- **Spec:** `e2e/tests/auth-extra.spec.ts` → `UAT-AUTH-02 weak passwords are rejected by the policy`
  - Assertions:
    - `expect( page.getByText(/at least\|must contain\|too short\|weak\|requirement/i).first(), 'a weak password must be refused with guidance', ).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: Change password action not offered; No password fields rendered

### UAT-AUTH-03 — P1 — Any

- **Plan step:** Idle for 5 minutes
- **Expected:** Auto-logout occurs, session banner shown
- **Spec:** `e2e/tests/auth-extra.spec.ts` → `UAT-AUTH-03 repeated bad passwords lock the account`
  - Assertions:
    - `expect( page.getByText(/locked\|too many (failed )?attempts\|temporarily/i).first(), 'the account must lock after repeated failures', ).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: none beyond the seeded baseline

### UAT-AUTH-04 — P1 — Any

- **Plan step:** Use "Forgot password" link
- **Expected:** Reset email delivered; new password logs in
- **Spec:** `e2e/tests/auth-extra.spec.ts` → `UAT-AUTH-04 an idle session is logged out automatically`
  - Assertions:
    - `expect(mounted).toBe(true)`
    - `expect.soft(banner).toBeVisible()`
    - `expect( page.getByRole('button', { name: /sign in\|log in/i }).first(), 'an expired session must return the user to sign-in', ).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: none beyond the seeded baseline

### UAT-AUTH-05 — P1 — CRO

- **Plan step:** Attempt to open `/user-management`
- **Expected:** Access Denied page shown
- **Spec:** `e2e/tests/negative-rbac.spec.ts` → `UAT-AUTH-05 CRO cannot open /user-management`
  - Assertions:
    - `expect(denied \|\| redirected).toBe(true)`
    - `expect(await link.count()).toBe(0)`
  - Required test data / skip guards: CRO credentials not configured
- **Spec:** `e2e/tests/sidebar-access.spec.ts` → `[${user.role}] is blocked from forbidden routes`
  - Assertions:
    - `expect.soft( denied \|\| !stillHere, `${user.role} unexpectedly reached forbidden path ${p}`, ).toBe(true)`
  - Required test data / skip guards: none beyond the seeded baseline

### UAT-AUTH-06 — P1 — RMD/ADMIN

- **Plan step:** Open `/user-management`
- **Expected:** Page loads with user list
- **Spec:** `e2e/tests/sidebar-access.spec.ts` → `[${user.role}] can open all allowed routes`
  - Assertions:
    - `expect.soft(denied, `${user.role} was denied on allowed path ${p}`).toBe(false)`
  - Required test data / skip guards: none beyond the seeded baseline

### UAT-AUTH-07 — P2 — ADMIN

- **Plan step:** Invite user with role RC
- **Expected:** User receives invite email; can complete signup
- **Spec:** `e2e/tests/auth-extra.spec.ts` → `UAT-AUTH-07 password reset can be requested from the sign-in page`
  - Assertions:
    - `expect( page.getByText(/sent\|check your (email\|inbox)\|reset link/i).first(), 'a reset request must be acknowledged', ).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: Forgot password entry point not rendered


## 5.2 Risk Register (Identify + Assess)

### UAT-REG-01 — P1 — RC

- **Plan step:** Create risk via 4-step wizard
- **Expected:** Risk saved as Draft, appears in "My Risks"
- **Spec:** `e2e/tests/risk-journey.spec.ts` → `UAT-RISK-01 RC completes the 4-step wizard and creates a risk`
  - Assertions:
    - `expect.soft(next).toBeDisabled()`
    - `expect(next).toBeEnabled({ timeout: 10_000 })`
    - `expect(page.getByText(/step 2 of 4/i)).toBeVisible()`
    - `expect.soft(page.getByRole('button', { name: /create risk now/i })).toBeVisible()`
    - `expect(page.getByText(/step 3 of 4/i)).toBeVisible()`
    - `expect(page.getByText(/step 4 of 4/i)).toBeVisible()`
    - `expect(page.getByText(title)).toBeVisible({ timeout: 20_000 })`
    - `expect(row.status, row.body).toBe(200)`
    - _…and 2 more_
  - Required test data / skip guards: No risk-authoring credentials configured

### UAT-REG-02 — P1 — RC

- **Plan step:** Submit draft risk
- **Expected:** Status → Submitted; RR/RMD inbox notified
- **Spec:** `e2e/tests/register-data.spec.ts` → `UAT-REG-02 a draft risk can be submitted for review`
  - Assertions:
    - `expect(dialog).toBeVisible({ timeout: 15_000 })`
    - `expect.soft(after.body, 'submitted risk should read Submitted').toMatch(/Submitted/i)`
  - Required test data / skip guards: No writer credentials configured; No Draft risk available to submit; Submit action not offered

### UAT-REG-03 — P1 — RMD

- **Plan step:** Create risk as RMD
- **Expected:** Risk saved successfully (regression: prior submit error)
- **Spec:** `e2e/tests/register-data.spec.ts` → `UAT-REG-03 RMD can author a risk (regression: prior submit error)`
  - Assertions:
    - `expect(page.getByText(/step 1 of 4/i)).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: No RMD credentials configured

### UAT-REG-04 — P1 — Any writer

- **Plan step:** Category dropdown
- **Expected:** Reflects live `risk_categories` table (no stale enum)
- **Spec:** `e2e/tests/register-data.spec.ts` → `UAT-REG-04 category dropdown is fed by the risk_categories table`
  - Assertions:
    - `expect(page.getByText(/step 1 of 4/i)).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: No writer credentials configured; No active risk categories seeded

### UAT-REG-05 — P1 — Any writer

- **Plan step:** Department dropdown
- **Expected:** Reflects live `departments` table
- **Spec:** `e2e/tests/register-data.spec.ts` → `UAT-REG-05 department dropdown is fed by the departments table`
  - Assertions:
    - `expect(page.getByText(/step 1 of 4/i)).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: No writer credentials configured; No departments seeded; Department control not on step 1

### UAT-REG-06 — P2 — RC

- **Plan step:** Bulk upload risks (CSV)
- **Expected:** Rows validated, invalid rows itemised, valid rows imported
- **Spec:** `e2e/tests/register-data.spec.ts` → `UAT-REG-06 CSV bulk upload validates rows before import`
  - Assertions:
    - `expect(dialog).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: No writer credentials configured; Bulk upload action not offered to this role; No file input rendered

### UAT-REG-07 — P2 — RC

- **Plan step:** Upload attachment to a risk
- **Expected:** File appears in Risk Document Vault; signed URL opens
- **Spec:** `e2e/tests/register-data.spec.ts` → `UAT-REG-07 an attachment uploads into the risk document vault`
  - Assertions:
    - `expect(dialog).toBeVisible({ timeout: 15_000 })`
    - `expect(dialog.getByText(name)).toBeVisible({ timeout: 20_000 })`
  - Required test data / skip guards: No writer credentials configured; No risk visible to this role; Attachment upload not offered

### UAT-REG-08 — P1 — RMD

- **Plan step:** Batch AI analysis on 5 risks
- **Expected:** Scores + rationales stored; no rate-limit crash
- **Spec:** `e2e/tests/register-data.spec.ts` → `UAT-REG-08 batch AI analysis completes without a rate-limit crash`
  - Assertions:
    - `expect( page.getByText(/complete\|analysed\|analyzed\|score/i).first(), ).toBeVisible({ timeout: 90_000 })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; Batch AI action not offered


## 5.3 Approvals (Review / Approve / Return / Escalate)

### UAT-APP-01 — P1 — RR

- **Plan step:** Claim a Submitted risk
- **Expected:** Status → Under Review; claim-lock prevents second RR claiming
- **Spec:** `e2e/tests/risk-journey.spec.ts` → `UAT-RISK-03 submit → claim → approve records approval history`
  - Assertions:
    - `expect(page.getByText(title)).toBeVisible({ timeout: 20_000 })`
    - `expect.soft(history.status, history.body).toBe(200)`
    - `expect.soft(history.body).toContain('approval_status')`
    - `expect(page.getByText(/step 1 of 4/i)).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: Author or approver credentials not configured; Risk did not reach the approval inbox in this environment

### UAT-APP-02 — P1 — CRO

- **Plan step:** Approve Under Review risk
- **Expected:** Status → Approved; approval_history entry recorded
- **Spec:** `e2e/tests/risk-journey.spec.ts` → `UAT-RISK-03 submit → claim → approve records approval history`
  - Assertions:
    - `expect(page.getByText(title)).toBeVisible({ timeout: 20_000 })`
    - `expect.soft(history.status, history.body).toBe(200)`
    - `expect.soft(history.body).toContain('approval_status')`
    - `expect(page.getByText(/step 1 of 4/i)).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: Author or approver credentials not configured; Risk did not reach the approval inbox in this environment

### UAT-APP-03 — P1 — RR

- **Plan step:** Return risk with comment
- **Expected:** Status → Returned; submitter notified with comment
- **Spec:** `e2e/tests/risk-journey.spec.ts` → `UAT-RISK-05 reviewer returns for revision, originator resubmits, approval history is ordered`
  - Assertions:
    - `expect(page.getByText(title)).toBeVisible({ timeout: 20_000 })`
    - `expect(riskRow.status, riskRow.body).toBe(200)`
    - `expect.soft(history.status, history.body).toBe(200)`
    - `expect.soft(rows.length, 'expected at least a submit + a return event').toBeGreaterThanOrEqual(2)`
    - `expect.soft(timestamps).toEqual(sorted)`
    - `expect(page.getByText(/step 1 of 4/i)).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: Author or approver credentials not configured; Risk did not reach the approval inbox in this environment; No return-for-revision control on this build

### UAT-APP-04 — P1 — RC

- **Plan step:** Withdraw before RR claims
- **Expected:** Status returns to Draft
- **Spec:** `e2e/tests/approvals-extra.spec.ts` → `UAT-APP-04 originator can withdraw a submitted risk before it is claimed`
  - Assertions:
    - `expect.soft(after.body, 'withdrawn risk returns to Draft').toMatch(/Draft/i)`
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RC/RO credentials configured; No Submitted risk available; Withdraw action not offered

### UAT-APP-05 — P1 — RC

- **Plan step:** Attempt to withdraw after RR claims
- **Expected:** Action blocked with explanatory toast
- **Spec:** `e2e/tests/approvals-extra.spec.ts` → `UAT-APP-05 withdrawal is blocked once a reviewer has claimed the risk`
  - Assertions:
    - `expect(after.body, 'claimed risk must stay Under Review').toMatch(/Under Review/i)`
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RC/RO credentials configured; No Under Review risk available

### UAT-APP-06 — P2 — CRO

- **Plan step:** Escalate an Approved risk
- **Expected:** Status → Escalated; executive users notified
- **Spec:** `e2e/tests/approvals-extra.spec.ts` → `UAT-APP-06 CRO escalates an approved risk`
  - Assertions:
    - `expect.soft(after.body, 'escalated risk should read Escalated').toMatch(/Escalated/i)`
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No CRO/RMD/ADMIN credentials configured; No Approved risk available; Escalate action not offered

### UAT-APP-07 — P2 — ADMIN

- **Plan step:** De-escalate
- **Expected:** Status returns to prior lifecycle
- **Spec:** `e2e/tests/approvals-extra.spec.ts` → `UAT-APP-07 ADMIN de-escalates back to the prior lifecycle state`
  - Assertions:
    - `expect.soft(after.body, 'de-escalated risk must leave Escalated').not.toMatch(/Escalated/i)`
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No ADMIN/CRO credentials configured; No Escalated risk available; De-escalate action not offered


## 5.4 Treatment & Mitigation

### UAT-TRT-01 — P1 — RO

- **Plan step:** Add mitigation task with budget in NGN
- **Expected:** Saved; appears in Mitigation Tasks panel
- **Spec:** `e2e/tests/treatment-journey.spec.ts` → `UAT-TRT-01 mitigation task with an NGN budget is saved and listed`
  - Assertions:
    - `expect(dialog.getByText(/mitigation task/i).first()).toBeVisible({ timeout: 10_000 })`
    - `expect(page.getByText(title)).toBeVisible({ timeout: 15_000 })`
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RO/RMD/ADMIN credentials configured; No risk visible to this role; Add Task action not offered to this role

### UAT-TRT-02 — P1 — RO

- **Plan step:** Complete all mitigation tasks
- **Expected:** Risk transitions to Mitigated
- **Spec:** `e2e/tests/treatment-journey.spec.ts` → `UAT-TRT-02 completing every mitigation task transitions the risk to Mitigated`
  - Assertions:
    - `expect(dialog).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RO/RMD/ADMIN credentials configured; No open mitigation task available to complete

### UAT-TRT-03 — P2 — RMD

- **Plan step:** Request AI mitigation recommendations
- **Expected:** Recommendations returned within 30s
- **Spec:** `e2e/tests/treatment-journey.spec.ts` → `UAT-TRT-03 AI mitigation recommendations return within 30s`
  - Assertions:
    - `expect( page.getByText(/recommend\|suggestion\|mitigation plan/i).first(), ).toBeVisible({ timeout: 30_000 })`
    - `expect(Date.now() - started, 'recommendations must return inside 30s').toBeLessThan(30_000)`
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No risk visible to this role; AI mitigation action not offered

### UAT-TRT-04 — P2 — RO

- **Plan step:** Budget utilisation crosses 75% / 90%
- **Expected:** Colour flips yellow / red; alert generated
- **Spec:** `e2e/tests/treatment-journey.spec.ts` → `UAT-TRT-04 budget utilisation thresholds flip the indicator colour`
  - Assertions:
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RO/RMD/ADMIN credentials configured; No risk visible to this role; Budget utilisation indicator not rendered


## 5.5 Dashboard, Matrix & Reports

### UAT-DSH-01 — P1 — CRO / RCB

- **Plan step:** Compare BCP % Coverage on same day
- **Expected:** Values match across roles (consistency fix)
- **Spec:** `e2e/tests/dashboard-consistency.spec.ts` → `UAT-DSH-01 BCP % coverage is identical across roles on the same day`
  - Assertions:
    - `expect( second, `BCP coverage must match across roles (${a!.role}=${first}%, ${b!.role}=${second}%)`, ).toBe(first)`
  - Required test data / skip guards: Need two roles with credentials to compare; BCP coverage card not rendered for both roles

### UAT-DSH-02 — P1 — Any viewer

- **Plan step:** Click "High Severity" card
- **Expected:** Drawer opens filtered to score ≥ 15
- **Spec:** `e2e/tests/reporting-journey.spec.ts` → `UAT-RPT-02 dashboard cards drill down into filtered views`
  - Assertions:
    - `expect.soft(body).not.toMatch(/\bNaN\b/)`
    - `expect.soft(url.pathname === '/app' && url.search === '').toBe(false)`
  - Required test data / skip guards: No dashboard credentials configured; No drill-down card found

### UAT-DSH-03 — P1 — Any viewer

- **Plan step:** Open Risk Matrix
- **Expected:** Heatmap renders < 2s with correct counts
- **Spec:** `e2e/tests/dashboard-consistency.spec.ts` → `UAT-DSH-03 the risk matrix heatmap renders inside the performance budget`
  - Assertions:
    - `expect(page.getByRole('heading', { name: /risk matrix\|heatmap/i }).first()).toBeVisible({ timeout: 15_000, })`
    - `expect(cells.first()).toBeVisible({ timeout: 15_000 })`
    - `expect.soft(elapsed, `heatmap should render in under 5s (took ${elapsed}ms)`).toBeLessThan(5000)`
    - `expect.soft(await cells.count(), 'heatmap must render cells').toBeGreaterThan(0)`
  - Required test data / skip guards: No credentials configured

### UAT-DSH-04 — P2 — CRO

- **Plan step:** Generate AI executive summary
- **Expected:** PDF opens with current metrics
- **Spec:** `e2e/tests/reporting-journey.spec.ts` → `UAT-RPT-03 executive roles reach the executive summary`
  - Assertions:
    - `expect(await page.getByText(/access denied/i).count()).toBe(0)`
  - Required test data / skip guards: No executive credentials configured

### UAT-DSH-05 — P2 — RCB

- **Plan step:** Generate all 5 board reports
- **Expected:** Each PDF downloads and prints correctly
- **Spec:** `e2e/tests/board-reports.spec.ts` → `UAT-BRPT-01 an authorised role generates a board report and downloads the PDF`
  - Assertions:
    - `expect(await page.getByText(/access denied/i).count()).toBe(0)`
    - `expect.soft(download, 'expected a PDF download to start').not.toBeNull()`
    - `if (download) expect.soft(download.suggestedFilename()).toMatch(/\.pdf$/i)`
  - Required test data / skip guards: No board-reporting credentials configured; No generate control on this build; No download control appeared after generation
- **Spec:** `e2e/tests/reporting-journey.spec.ts` → `UAT-RPT-01 board report can be generated and archived`
  - Assertions:
    - `expect.soft(archive.status, archive.body).toBe(200)`
  - Required test data / skip guards: No reporting credentials configured; No generate control on this build

### UAT-DSH-06 — P3 — Any

- **Plan step:** Export dashboard PDF/PNG
- **Expected:** File opens intact
- **Spec:** `e2e/tests/dashboard-consistency.spec.ts` → `UAT-DSH-06 dashboard export produces a file`
  - Assertions:
    - `expect.soft(download, 'an export file should be produced').not.toBeNull()`
    - `expect.soft(download.suggestedFilename()).toMatch(/\.(pdf\|png\|csv)$/i)`
  - Required test data / skip guards: No credentials configured; Export action not offered


## 5.6 Business Continuity (BCP + BIA)

### UAT-BCP-01 — P1 — RMD

- **Plan step:** Create BCP with today's BIA date
- **Expected:** Saved; BIA date defaults to today
- **Spec:** `e2e/tests/bcp-journey.spec.ts` → `UAT-BCP-01 create a plan with RTO/RPO and mitigation actions`
  - Assertions:
    - `expect(page.getByText(name)).toBeVisible({ timeout: 20_000 })`
    - `expect(row.status, row.body).toBe(200)`
    - `expect(row.body).not.toBe('[]')`
  - Required test data / skip guards: No BCP-authoring credentials configured

### UAT-BCP-02 — P1 — RMD

- **Plan step:** Save BCP with invalid impact rating
- **Expected:** Inline field error shown, save blocked
- **Spec:** `e2e/tests/bcp-journey.spec.ts` → `UAT-BCP-02 BIA and test-detail validation errors render inline`
  - Assertions:
    - `expect.soft(await inline.count(), 'expected an inline validation message').toBeGreaterThan(0)`
  - Required test data / skip guards: No BCP-authoring credentials configured; No existing BCP available to edit; BIA section not present in this build

### UAT-BCP-03 — P1 — RMD

- **Plan step:** Edit BCP → change RTO
- **Expected:** Entry appears in Version History panel
- **Spec:** `e2e/tests/bcp-journey.spec.ts` → `UAT-BCP-03 editing a plan writes version history`
  - Assertions:
    - `expect.soft(history.status, history.body).toBe(200)`
    - `expect.soft(history.body).not.toBe('[]')`
  - Required test data / skip guards: No BCP-authoring credentials configured; No existing BCP available to edit

### UAT-BCP-04 — P2 — RMD

- **Plan step:** Log a test with results & findings
- **Expected:** Persists; retrievable in Test Details
- **Spec:** `e2e/tests/closure-workflows.spec.ts` → `UAT-BCP-04 test findings are logged and tracked to closure`
  - Assertions:
    - `expect(page.getByRole('heading', { name: /business continuity/i }).first()).toBeVisible({ timeout: 15_000, })`
  - Required test data / skip guards: No RMD/ADMIN/CRO credentials configured; No BCP tests logged yet

### UAT-BCP-05 — P2 — ADMIN

- **Plan step:** Open `/bcp-schema-checks`
- **Expected:** Filterable log of startup verifications
- **Spec:** `e2e/tests/bcp-journey.spec.ts` → `UAT-BCP-04 schema-check log page is admin/RMD only`
  - Assertions:
    - `expect(denied > 0 \|\| redirected).toBe(true)`
  - Required test data / skip guards: No restricted-role credentials configured


## 5.7 Incidents & Crystallised Risks

### UAT-INC-01 — P1 — RO

- **Plan step:** Report crystallised risk
- **Expected:** Event stored; executives notified
- **Spec:** `e2e/tests/incident-journey.spec.ts` → `UAT-INC-03 incident owner can be assigned and the incident closed`
  - Assertions: _none captured (navigational / setup step)_
  - Required test data / skip guards: No incident-managing credentials configured; No incidents available

### UAT-INC-02 — P2 — RMD

- **Plan step:** Incident timeline for a risk
- **Expected:** Chronological events render correctly
- **Spec:** `e2e/tests/incident-journey.spec.ts` → `UAT-INC-05 reassigning the owner is recorded in the timeline, and the notification deep-link opens the incident`
  - Assertions:
    - `expect.soft(await timeline.count()).toBeGreaterThan(0)`
  - Required test data / skip guards: No incident-managing credentials configured; No incidents available; No owner-reassignment control on this build; No alternate owner available to assign


## 5.8 Whistleblowing

### UAT-WB-01 — P1 — Anonymous

- **Plan step:** Submit case (no login)
- **Expected:** Case ID + follow-up code issued
- **Spec:** `e2e/tests/whistleblow-journey.spec.ts` → `UAT-WB-01 anonymous report can be submitted without a session`
  - Assertions:
    - `expect(page.getByRole('heading', { name: /whistleblow\|report\|speak up/i }).first()).toBeVisible({ timeout: 15_000, })`
    - `expect.soft(await page.getByRole('option').count(), 'category list must not be empty').toBeGreaterThan(0)`
    - `expect(confirmation).toBeVisible({ timeout: 30_000 })`
  - Required test data / skip guards: Whistleblow form not reachable in this build; No submit control found

### UAT-WB-02 — P1 — Anonymous

- **Plan step:** Return with follow-up code
- **Expected:** Reads status / adds reply, submitter identity NOT visible
- **Spec:** `e2e/tests/whistleblow-journey.spec.ts` → `UAT-WB-02 follow-up requires a valid token`
  - Assertions:
    - `expect.soft(await page.getByText(/not found\|invalid\|no case/i).count()).toBeGreaterThan(0)`
  - Required test data / skip guards: Follow-up page not present in this build

### UAT-WB-03 — P1 — RMD/CRO

- **Plan step:** Investigation workspace
- **Expected:** Case shows SLA age; >14d unassigned auto-flag
- **Spec:** `e2e/tests/whistleblow-journey.spec.ts` → `UAT-WB-03 supervisor can triage cases; others cannot`
  - Assertions:
    - `expect(await page.getByText(/access denied/i).count()).toBe(0)`
  - Required test data / skip guards: No supervisor credentials configured

### UAT-WB-04 — P2 — RMD

- **Plan step:** Close case with outcome
- **Expected:** Status = Closed; audit trail complete
- **Spec:** `e2e/tests/closure-workflows.spec.ts` → `UAT-WB-04 a whistleblow case is closed with an outcome`
  - Assertions:
    - `expect.soft(after.body, 'the case must read Closed').toMatch(/Closed/i)`
  - Required test data / skip guards: No SUPERVISOR/ADMIN/CRO credentials configured; No open whistleblow case available; Close action not offered


## 5.9 Audit Logs

### UAT-AUD-01 — P1 — RMD/CRO

- **Plan step:** Open `/audit-logs` → Risk Changes
- **Expected:** List of diffs (who/when/what) shown
- **Spec:** `e2e/tests/audit-logs.spec.ts` → `UAT-AUD-01 RMD/CRO see the Risk Changes diff history`
  - Assertions:
    - `expect(await isAccessDenied(page), `${user!.role} must reach /audit-logs`).toBe(false)`
    - `expect(tab).toBeVisible({ timeout: 15_000 })`
    - `expect.soft(page.getByText(col).first()).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured

### UAT-AUD-02 — P1 — RMD

- **Plan step:** Apply date-range + sort + page size, then refresh
- **Expected:** Preferences persist
- **Spec:** `e2e/tests/audit-logs.spec.ts` → `UAT-AUD-02 date range, sort and page size survive a reload`
  - Assertions:
    - `expect(tab).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; Page size control not rendered

### UAT-AUD-03 — P2 — RC

- **Plan step:** Attempt to open `/audit-logs`
- **Expected:** Access Denied
- **Spec:** `e2e/tests/audit-logs.spec.ts` → `UAT-AUD-03 RC is denied access to /audit-logs`
  - Assertions:
    - `expect(denied \|\| redirected, `${user!.role} must not reach /audit-logs`).toBe(true)`
  - Required test data / skip guards: No RC/RO/RR credentials configured


## 5.10 Notifications

### UAT-NTF-01 — P1 — RC

- **Plan step:** Task due in ≤ 7 days
- **Expected:** Notification arrives at 08:00
- **Spec:** `e2e/tests/notifications.spec.ts` → `UAT-NTF-01 unread count decrements when a notification is read`
  - Assertions:
    - `expect(panel).toBeVisible({ timeout: 10_000 })`
    - `expect.soft(remaining, 'unread count must drop after marking read').toBeLessThan(rows.length)`
  - Required test data / skip guards: No credentials configured; No unread notifications for this user; Mark-as-read action not offered

### UAT-NTF-02 — P1 — RO

- **Plan step:** Task overdue
- **Expected:** Escalation notification received
- **Spec:** `e2e/tests/notifications.spec.ts` → `UAT-NTF-02 a notification deep-links to its related record`
  - Assertions: _none captured (navigational / setup step)_
  - Required test data / skip guards: No credentials configured; No linkable notification present

### UAT-NTF-03 — P2 — Any

- **Plan step:** Notification bell
- **Expected:** Unread count matches unread list
- **Spec:** `e2e/tests/notifications.spec.ts` → `UAT-NTF-03 quiet hours and mutes suppress realtime pop-ups`
  - Assertions:
    - `expect(prefs).toBeVisible({ timeout: 10_000 })`
    - `expect.soft(after, 'the mute toggle must change state').not.toBe(before)`
  - Required test data / skip guards: No credentials configured; Notification preferences not rendered on the profile page


## 5.11 Settings & Administration

### UAT-SET-01 — P1 — ADMIN

- **Plan step:** Add new risk category
- **Expected:** Appears in dropdown immediately (enum-sync trigger)
- **Spec:** `e2e/tests/settings-config.spec.ts` → `UAT-SET-01 a new risk category is usable immediately in the wizard`
  - Assertions:
    - `expect(await isAccessDenied(page), `${user!.role} should reach /settings`).toBe(false)`
    - `expect(page.getByText(name)).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No ADMIN/RMD/CRO credentials configured; Categories tab not offered; Add category action not offered

### UAT-SET-02 — P1 — ADMIN

- **Plan step:** Edit risk appetite thresholds
- **Expected:** Reflected in dashboard breach indicators
- **Spec:** `e2e/tests/settings-config.spec.ts` → `UAT-SET-02 the scoring matrix drives displayed severity bands`
  - Assertions:
    - `expect.soft(matrix.status, 'scoring matrix must be readable').toBeLessThan(400)`
    - `expect.soft(rows.length, 'a scoring matrix must be configured').toBeGreaterThan(0)`
    - `expect(page.getByRole('heading', { name: /risk matrix\|heatmap/i }).first()).toBeVisible({ timeout: 15_000, })`
    - `expect.soft(page.getByText(/high\|critical/i).first()).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: No ADMIN/RMD/CRO credentials configured

### UAT-SET-03 — P2 — ADMIN

- **Plan step:** Load / clear sample data
- **Expected:** Operations succeed with confirmation
- **Spec:** `e2e/tests/settings-config.spec.ts` → `UAT-SET-03 email templates are editable with placeholders per event type`
  - Assertions:
    - `expect(page.getByText(/template/i).first()).toBeVisible({ timeout: 10_000 })`
    - `expect.soft(templates.status, 'templates must be readable by administrators').toBeLessThan(400)`
  - Required test data / skip guards: No ADMIN/RMD/CRO credentials configured; Email templates tab not offered


## 5.12 Key Risk Indicators (W1)

**Section test data:** two seeded risks (one Operational, one Compliance), an "higher is worse" indicator (`unit = %`, warning 5, critical 10, Monthly) and a "lower is worse" indicator (`unit = days`, warning 3, critical 1, Weekly). RMD, CRO, RC and RO accounts required.

### UAT-KRI-01 — P1 — RMD

- **Plan step:** Open `/kris` → **New Indicator**, set unit, warning and critical thresholds, breach direction and frequency
- **Expected:** Indicator saved and listed with status **No Data**
- **Spec:** `e2e/tests/kri-readings.spec.ts` → `UAT-KRI-01 RMD creates an indicator with thresholds, direction and frequency`
  - Assertions:
    - `expect(dialog).toBeVisible({ timeout: 10_000 })`
    - `expect.soft(dialog.getByText(/direction\|higher is worse\|above/i).first()).toBeVisible()`
    - `expect.soft(dialog.getByText(/frequency\|monthly\|weekly/i).first()).toBeVisible()`
    - `expect(page.getByText(name)).toBeVisible({ timeout: 20_000 })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured

### UAT-KRI-02 — P1 — RMD

- **Plan step:** Record a reading below the warning threshold ("higher is worse", e.g. 2%)
- **Expected:** Status becomes **Normal**; trend chart plots the point
- **Spec:** `e2e/tests/kri-readings.spec.ts` → `UAT-KRI-02 a reading below the warning threshold stays Normal`
  - Assertions:
    - `expect(dialog.getByText(/classified as/i)).toBeVisible({ timeout: 8000 })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No "higher is worse" indicator seeded; Record action not offered

### UAT-KRI-03 — P1 — RMD

- **Plan step:** Record a reading at/above the warning threshold (e.g. 6%)
- **Expected:** Status becomes **Warning**; owner, RMD and CRO receive an in-app notification
- **Spec:** `e2e/tests/kri-readings.spec.ts` → `UAT-KRI-03 recording a reading derives Normal / Warning / Critical`
  - Assertions:
    - `expect(page.getByRole('heading', { name: /key risk indicators/i })).toBeVisible({ timeout: 15_000, })`
    - `expect(dialog.getByText(/record reading/i)).toBeVisible({ timeout: 10_000 })`
    - `expect(page.getByText(/reading recorded/i)).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No indicator available to record against

### UAT-KRI-04 — P1 — CRO

- **Plan step:** Record a reading at/above the critical threshold (e.g. 12%)
- **Expected:** Status becomes **Critical**; breach notification raised and KPI "Critical" count increases by 1
- **Spec:** `e2e/tests/kri-readings.spec.ts` → `UAT-KRI-04 breach direction is honoured in both directions`
  - Assertions: _none captured (navigational / setup step)_
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No indicators seeded (see UAT test data, Section 8)

### UAT-KRI-05 — P2 — RMD

- **Plan step:** Record a falling value (e.g. 1 day) against the "lower is worse" indicator
- **Expected:** Warning/Critical derive in the inverted direction
- **Spec:** `e2e/tests/kri-readings.spec.ts` → `UAT-KRI-05 a critical reading notifies the owner, RMD and CRO`
  - Assertions:
    - `expect.soft(count, 'a KRI breach must raise a notification').toBeGreaterThan(0)`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No critical reading recorded yet

### UAT-KRI-06 — P2 — RMD

- **Plan step:** Set a past **next reading due** date
- **Expected:** Indicator counts under **Reading overdue** KPI and the date shows in red
- **Spec:** `e2e/tests/kri-readings.spec.ts` → `UAT-KRI-06 the trend chart plots the reading history`
  - Assertions:
    - `expect(page.getByRole('heading', { name: /key risk indicators/i })).toBeVisible({ timeout: 15_000, })`
  - Required test data / skip guards: No credentials configured; No readings recorded to chart

### UAT-KRI-07 — P2 — RC / RO

- **Plan step:** Open `/kris`
- **Expected:** Register loads read-only — **New Indicator**, **Record reading** and **Edit** controls are absent (recording is an RMD/CRO/ADMIN action)
- **Spec:** `e2e/tests/kri-governance.spec.ts` → `UAT-KRI-01 [${user.role}] /kris route and action gating match the matrix`
  - Assertions:
    - `expect.soft(denied, `${user.role} unexpectedly denied on /kris`).toBe(false)`
    - `expect(heading).toBeVisible({ timeout: 10_000 })`
    - `expect.soft(newBtn, `${user.role} should see New Indicator`).toBeVisible()`
  - Required test data / skip guards: none beyond the seeded baseline

### UAT-KRI-08 — P2 — ERMSC / EC / RCB

- **Plan step:** Open `/kris`
- **Expected:** Indicators and trends are visible for oversight; no editing controls offered
- **Spec:** `e2e/tests/kri-readings.spec.ts` → `UAT-KRI-08 executives can view but never edit indicators`
  - Assertions:
    - `expect(page.getByRole('heading', { name: /key risk indicators/i })).toBeVisible({ timeout: 15_000, })`
    - `expect(canPerformAction(user!.role, 'kri.manage'), 'executives are read-only').toBe(false)`
    - `expect(page.getByRole('button', { name: /new indicator/i })).toHaveCount(0)`
    - `expect(page.getByRole('button', { name: /^record$/i })).toHaveCount(0)`
  - Required test data / skip guards: No executive credentials configured

### UAT-KRI-09 — P2 — RMD

- **Plan step:** Link an indicator to a risk and a category
- **Expected:** Linkage shows on the register row, opens the linked risk, and survives reload
- **Spec:** `e2e/tests/kri-readings.spec.ts` → `UAT-KRI-09 an indicator links back to its parent risk`
  - Assertions:
    - `expect(page.getByText(row!.name!)).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No credentials configured; No indicator linked to a risk; No E2E user credentials configured


## 5.13 Governance & Committee Decisions (W4)

**Section test data:** one Approved risk with residual score ≥ 15, one Escalated risk, and accounts for CRO, ERMSC, RCB, RMD and RC. Minute references use the UAT convention `ERMSC/UAT/001`.

### UAT-GOV-01 — P1 — CRO

- **Plan step:** Open a risk → **Committee Decisions** → record an **Accept** decision from ERMSC with rationale and minute reference
- **Expected:** Decision saved with forum, date, rationale; risk treatment strategy is stamped **Accept**
- **Spec:** `e2e/tests/governance-decisions.spec.ts` → `UAT-GOV-01 an ERMSC Accept decision is recorded with rationale and minute reference`
  - Assertions:
    - `expect.soft(saved.status, saved.body).toBeLessThan(400)`
    - `expect.soft(saved.body, 'the decision must be persisted against the risk').not.toBe('[]')`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No risk visible to this role; Record decision action not offered

### UAT-GOV-02 — P1 — ERMSC

- **Plan step:** Record an **Escalate** decision with a minute reference
- **Expected:** Entry appears at the top of the decision history with the recorder's name and timestamp
- **Spec:** `e2e/tests/governance-decisions.spec.ts` → `UAT-GOV-02 an ERMSC Escalate decision is recorded with a minute reference`
  - Assertions:
    - `expect.soft(saved.status, saved.body).toBeLessThan(400)`
    - `expect.soft(saved.body, 'the decision must be persisted against the risk').not.toBe('[]')`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No risk visible to this role; Record decision action not offered

### UAT-GOV-03 — P1 — CRO

- **Plan step:** Save a decision with a rationale shorter than 10 characters
- **Expected:** Save is rejected with an inline message (server-side rule, not just client validation)
- **Spec:** `e2e/tests/kri-governance.spec.ts` → `UAT-GOV-02 [RMD] recording a decision requires a rationale of 10+ chars`
  - Assertions:
    - `expect(dialog).toBeVisible({ timeout: 15_000 })`
    - `expect( dialog.getByText(/rationale must be at least 10 characters/i), ).toBeVisible()`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No risks visible — cannot open view dialog; No E2E user credentials configured

### UAT-GOV-04 — P2 — RMD

- **Plan step:** Record a **Defer** decision with a review date
- **Expected:** Review date is displayed on the decision record and is visible to executives
- **Spec:** `e2e/tests/governance-decisions.spec.ts` → `UAT-GOV-04 an Accept decision stamps the risk treatment strategy`
  - Assertions:
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No risk visible to this role; Record decision action not offered

### UAT-GOV-05 — P2 — RC / RO

- **Plan step:** Open the same risk
- **Expected:** Decision history is visible but no **Record decision** button is offered
- **Spec:** `e2e/tests/kri-governance.spec.ts` → `UAT-GOV-01 [${role}] Record decision visibility matches the matrix`
  - Assertions:
    - `expect(dialog).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No risks visible to this role — cannot open view dialog

### UAT-GOV-06 — P2 — RCB

- **Plan step:** Generate the board report covering the risk
- **Expected:** Committee actions are traceable against the risk in the report
- **Spec:** `e2e/tests/governance-decisions.spec.ts` → `UAT-GOV-06 the decision history is append-only and shows the minute reference`
  - Assertions:
    - `expect(dialog).toBeVisible({ timeout: 15_000 })`
    - `expect(dialog.getByText(/committee decisions/i)).toBeVisible({ timeout: 10_000 })`
    - `expect.soft(dialog.getByText(new RegExp(row!.forum!, 'i')).first()).toBeVisible({ timeout: 10_000, })`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No governance decision recorded yet

### UAT-GOV-07 — P2 — RMD

- **Plan step:** Open `/audit-logs` → Risk Changes after UAT-GOV-01
- **Expected:** The treatment change caused by the committee decision is recorded with who/when/what
- **Spec:** `e2e/tests/governance-decisions.spec.ts` → `UAT-GOV-07 executives can read decisions but cannot record them`
  - Assertions:
    - `expect.soft(dialog.getByText(/committee decisions/i)).toBeVisible({ timeout: 10_000 })`
    - `expect(dialog.getByRole('button', { name: /record decision/i })).toHaveCount(0)`
    - `expect.soft(saved.status, saved.body).toBeLessThan(400)`
    - `expect.soft(saved.body, 'the decision must be persisted against the risk').not.toBe('[]')`
    - `expect(page.getByRole('dialog')).toBeVisible({ timeout: 15_000 })`
  - Required test data / skip guards: No executive credentials configured; No risk visible to this role; No E2E user credentials configured; No RMD/CRO/ADMIN credentials configured; Record decision action not offered


## 5.14 Risk Appetite Monitoring (W1)

**Section test data:** appetite configuration rows for at least two categories (one tolerant, one low-tolerance with threshold 12) plus a risk that can be re-scored above the limit.

### UAT-RAP-01 — P1 — ADMIN

- **Plan step:** Configure appetite thresholds per category in Settings (ICT executes the configuration step)
- **Expected:** Values persist and are shown in the appetite panel
- **Spec:** `e2e/tests/appetite.spec.ts` → `UAT-RAP-01 ADMIN configures appetite thresholds per category`
  - Assertions:
    - `expect(page.getByText(/risk appetite/i).first()).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: No ADMIN/RMD credentials configured; Risk appetite tab not offered

### UAT-RAP-02 — P1 — RMD

- **Plan step:** Validate the configured values against the approved appetite statement
- **Expected:** Configured thresholds match the approved statement (governance validation)
- **Spec:** `e2e/tests/appetite.spec.ts` → `UAT-RAP-02 configured thresholds match the approved appetite statement`
  - Assertions:
    - `expect.soft(r.threshold_score).toBeLessThanOrEqual(25)`
    - `expect.soft(r.tolerance_level, 'each row needs a tolerance level').toBeTruthy()`
    - `expect.soft(r.escalation_action, 'each row needs an escalation action').toBeTruthy()`
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No appetite rows seeded (see UAT test data, Section 8)

### UAT-RAP-03 — P1 — RMD

- **Plan step:** Raise a risk score past the category appetite
- **Expected:** Risk is flagged as outside appetite automatically; escalation action fires (notify / escalate / flag for audit)
- **Spec:** `e2e/tests/appetite.spec.ts` → `UAT-RAP-03 a risk scored past appetite is flagged automatically`
  - Assertions: _none captured (navigational / setup step)_
  - Required test data / skip guards: No RMD/CRO/ADMIN credentials configured; No appetite threshold configured; Appetite columns not exposed to this role; No risk currently scored above appetite

### UAT-RAP-04 — P2 — CRO

- **Plan step:** Review out-of-appetite risks alongside Critical KRIs
- **Expected:** Both surfaces agree on the same exposure picture
- **Spec:** `e2e/tests/appetite.spec.ts` → `UAT-RAP-04 out-of-appetite risks and critical KRIs tell the same story`
  - Assertions:
    - `expect(page.getByRole('heading', { name: /key risk indicators/i })).toBeVisible({ timeout: 15_000, })`
    - `expect.soft(tile).toBeVisible({ timeout: 10_000 })`
  - Required test data / skip guards: No CRO/RMD/ADMIN credentials configured

### UAT-RAP-05 — P2 — RMD

- **Plan step:** Change an appetite threshold after risks exist
- **Expected:** Existing risks are re-scanned and flags update without manual edits
- **Spec:** `e2e/tests/appetite.spec.ts` → `UAT-RAP-05 changing a threshold re-scans existing risks`
  - Assertions: _none captured (navigational / setup step)_
  - Required test data / skip guards: No ADMIN/RMD credentials configured; No appetite row to modify; This role may not modify appetite configuration


## Extra automated checks (security depth / negative paths, no plan case)

| Annotation | Spec | Test |
| --- | --- | --- |
| `UAT-AUTH-01-neg` | `auth.spec.ts` | UAT-AUTH-01-neg rejects invalid credentials |
| `UAT-BCP-extra-diff` | `bcp-journey.spec.ts` | UAT-BCP-05 editing BIA fields twice shows a diff with author and timestamp ordering |
| `UAT-DSH-neg` | `board-reports.spec.ts` | UAT-BRPT-02 an unauthorised role cannot reach the board reports page |
| `UAT-INC-extra-table` | `incident-journey.spec.ts` | UAT-INC-01 incidents table supports search, filter and URL state |
| `UAT-INC-extra-csv` | `incident-journey.spec.ts` | UAT-INC-02 CSV export is offered and triggers a download |
| `UAT-INC-extra-deeplink` | `incident-journey.spec.ts` | UAT-INC-06 a notification deep-link opens the target incident |
| `UAT-INC-neg` | `incident-journey.spec.ts` | UAT-INC-04 read-only roles cannot create incidents |
| `UAT-KRI-extra-dialog` | `kri-governance.spec.ts` | UAT-KRI-02 [RMD] can open the New Indicator dialog |
| `UAT-RLS-KRI` | `kri-governance.spec.ts` | UAT-RLS-KRI [${user.role}] crafted INSERTs are refused for non-mandated roles |
| `UAT-REG-neg` | `negative-rbac.spec.ts`, `risk-journey.spec.ts` | UAT-REG-neg [${role}] cannot see Add Risk action; UAT-RISK-04 read-only roles cannot open the wizard |
| `UAT-DATA-neg` | `negative-rbac.spec.ts` | UAT-DATA-neg RC cannot update a risk they do not own via Data API |
| `UAT-WB-neg` | `negative-rbac.spec.ts` | UAT-WB-neg [${role}] cannot open /whistleblow/cases |
| `UAT-REG-01-neg` | `risk-journey.spec.ts` | UAT-RISK-02 wizard blocks progress when mandatory fields are missing |
| `UAT-RLS-01` | `rls-matrix.spec.ts` | UAT-RLS-01 [${user.role}] table access matches the RLS matrix |
| `UAT-RLS-02` | `rls-matrix.spec.ts` | UAT-RLS-02 non-admin cannot grant themselves ADMIN |
| `UAT-SMOKE-extra-routes` | `role-smoke.spec.ts` | ${user.role} route access matches the permission matrix |
| `UAT-SMOKE-extra-sidebar` | `role-smoke.spec.ts` | ${user.role} sidebar links match canSeeInSidebar() |
| `UAT-SMOKE-extra-cta` | `role-smoke.spec.ts` | ${user.role} gated CTAs match canPerformAction() |
| `UAT-SMOKE-extra-coverage` | `role-smoke.spec.ts` | smoke matrix covers the configured roles |
| `UAT-WB-extra-followup` | `whistleblow-journey.spec.ts` | UAT-WB-04 anonymous follow-up with reference and passphrase can send a message; a wrong passphrase is rejected |
